Trigger: !commands

Content:

This is a list of available commands.

**__Role Commands__**
`.iam Server #` Replace hash with your server, 1, 6, 12, 17 or 20 for global, and korea for korea.
`.iam KL ##` Replace hash with stage role. You can find all self-assignable roles in pinned post.
`.iam Guild Leader` If you are Guild Leader and want to be pinged in Guild Recruitment Channels
> To be used in channel #commands

**__General Help Guides__**
`!artifact` ,   `!artifacttrans` ,   `!artbuilds`,  `!airshipmeta`,
`!guildraid`,   `!currency`,   `!gamespeed`,   `!guildinvasion`,
`!guilds`,   `!guildwar`,   `!help`,   `!pets`,
`!raidarts`,   `!roles`,   `!unit`,   `!unitlevels`,
`!unitmeta`,   `!unlocks`,   `!crypto`,   `!commands`
